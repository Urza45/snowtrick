{{ form_start(formUser) }}
<div class="container">
	<div class="row">
		<div class="col">
			{{ form_row(formUser.pseudo, {
			'label': 'Identifiant :', 
			'attr': {
				'placeholder': 'Identifiant',
				'class': 'form-control'
			}
		    }) }}
			{{ form_row(formUser.lastName, {
			'label': 'Nom', 
			'attr': {
				'placeholder': 'Votre nom',
				'class': 'form-control'
			}
		    }) }}
			{{ form_row(formUser.firstName, {
			'label': 'Prénom', 
			'attr': {
				'placeholder': 'Votre prénom',
				'class': 'form-control'
			}
		    }) }}
		</div>
		<div class="col">
			{{ form_row(formUser.email, {
			'label': 'Email :', 
			'attr': {
				'placeholder': 'Votre email',
				'class': 'form-control'
			}
		    }) }}
			{{ form_row(formUser.phone, {
			'label': 'Télephone :', 
			'attr': {
				'placeholder': 'Votre téléphone fixe',
				'class': 'form-control'
			}
		    }) }}
			{{ form_row(formUser.cellPhone, {
			'label': 'Portable', 
			'attr': {
				'placeholder': 'Votre téléphone portable',
				'class': 'form-control'
			}
		    }) }}
		</div>
		<div class="col">
			<input type="button" class="btn btn-lg btn-primary" value="Changer de mot de passe"><br/>
			<br/>
			<input type="button" class="btn btn-lg btn-primary" value="Changer de photographie de profil"><br/>
			<br/>
			<button class="btn btn-lg btn-primary" type="submit">
				Enregistrer
			</button>
		</div>
	</div>
</div>
{{ form_end(formUser) }}
